"use strict";

// const StackParser = require('./StackParser');
it('', function () {
  var stack = require('./stack');

  console.log('stack ->', stack); // const p = new StackParser('hello');
  // expect(p.parse()).toBe();
});
